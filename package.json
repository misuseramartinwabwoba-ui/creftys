{
  "name": "creftys",
  "private": true,
  "version": "1.0.0",
  "packageManager": "pnpm@10.28.2",
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r exec tsc --noEmit",
    "lint": "pnpm -r lint",
    "clean": "rm -rf packages/*/dist",
    "lint:deps": "node scripts/dependency-lint.js"
  },
  "dependencies": {
    "date-fns": "^4.2.1",
    "uuid": "^14.0.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^25.9.0",
    "eslint": "^10.4.0",
    "tsup": "^8.5.1",
    "typescript": "^6.0.3"
  }
}